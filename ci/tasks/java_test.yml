---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 8-jdk

inputs:
  - name: repo

outputs:
  -

run:
  path: repo/ci/scripts/java_test.sh

caches:
  - path: .gradle/
  - path: .m2/
